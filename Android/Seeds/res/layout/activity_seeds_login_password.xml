<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"    
    android:paddingLeft="10dip"
    android:paddingRight="10dip" >
    
    <!-- android:background="@drawable/password_welcome_bg" -->

    <TextView
        android:id="@+id/login_toast"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="20dip"
        android:gravity="center"
        android:text="@string/seeds_password_note"
        android:textColor="@android:color/black"
        android:textSize="20sp" />

    <com.simplelife.seeds.android.utils.password.LocusPassWordView
        android:id="@+id/mLocusPassWordView"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent" >
    </com.simplelife.seeds.android.utils.password.LocusPassWordView>

    <Button
        android:id="@+id/tvNoSetPassword"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center"
        android:paddingLeft="15dip"
        android:paddingRight="15dip"
        android:textColor="@android:color/black"
        android:text="@string/seeds_password_startdraw"
        android:visibility="gone" >
    </Button>

</FrameLayout>